const dev = {
    apiGateway: {
      URL: "dev",
    },
  };
  
const prod = {
    apiGateway: {
      URL: "prod",
    },
  };

const stag = {
    apiGateway: {
      URL: "stag",
    },
  };

  export {dev, prod, stag};

  